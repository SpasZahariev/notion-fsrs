name: Notion FSRS Review

on:
  workflow_dispatch:
    inputs:
      num_items:
        description: "Number of items to select"
        required: false
        default: "3"
      dry_run:
        description: "Dry run (preview only, no Notion updates)"
        required: false
        default: "false"

# Optional: uncomment for automatic daily runs at 8am UTC
# schedule:
#   - cron: '0 8 * * *'

jobs:
  remember-these:
    runs-on: ubuntu-24.04-arm
    timeout-minutes: 5

    steps:
      - uses: actions/checkout@v4

      - name: Install dependencies
        run: pip install -r requirements.txt

      - name: Run review scheduler
        env:
          NOTION_API_KEY: ${{ secrets.NOTION_API_KEY }}
        run: |
          DRY_RUN_FLAG=""
          if [ "${{ github.event.inputs.dry_run }}" = "true" ]; then
            DRY_RUN_FLAG="--dry-run"
          fi
          python main.py -n ${{ github.event.inputs.num_items }} $DRY_RUN_FLAG
