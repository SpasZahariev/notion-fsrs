[project]
name = "notion-fsrs"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.14"
dependencies = [
    "fsrs>=0.7.0",
    "httpx>=0.28.0",
    "python-dotenv>=1.0.0",
]
